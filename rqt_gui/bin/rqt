#!/usr/bin/python3
import os
import sys
import subprocess

p = subprocess.getoutput('rospack find rqt_gui').strip()
sys.path.append(os.path.join(p, 'src'))

from rqt_gui.main import Main
main = Main(filename=os.path.abspath(__file__))
sys.exit(main.main())
