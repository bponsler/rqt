#!/usr/bin/python3
import os
import sys
import subprocess

import rospkg

rp = rospkg.RosPack()
p = rp.get_path("rqt_gui")
sys.path.append(os.path.join(p, 'src'))

from rqt_gui.main import Main
main = Main(filename=os.path.abspath(__file__))
sys.exit(main.main())
